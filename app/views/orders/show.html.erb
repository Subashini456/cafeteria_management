<h4>hello</h4>

<% @order.each do |item| %>
  <tr>
      <td>Order id :<%= item.id %>, </td>
      <td>User id : <%= item.user_id %>, </td>
      <td>Total : <%= item.total %>, </td>
    <% if item.status == "Placed" %>
      <td>Status : <%= item.status %> </td>
      <%= link_to '<button class="Menu-button" type="button">Delivered</button>'.html_safe, order_path(item.id), method: :put,:onclick => "alert('Are U sure')" %>
    <% else %>
      <td>Status : <%= item.status %> </td>
    <% end %>
  </tr>
  <br/>
<% end %>
